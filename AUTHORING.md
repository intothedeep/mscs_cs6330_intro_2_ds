# 책 집필 지시사항 (Authoring Guide)

이 문서는 이 저장소의 **모든 책**(`book_bio`, `book_ml`, `book_algo`, 이후 추가되는 책 포함)을
쓸 때 따라야 할 지시사항이다. 새 글을 쓰기 전에 이 문서를 먼저 확인한다.

> 이 파일은 저자(사용자)의 지시를 모아 둔 것이다. 코드/빌드 규칙은 `.claude/CLAUDE.md`,
> 각 책의 진행 상황은 해당 책 폴더를 참고한다.

---

## 1. 글쓰기 원칙 (내용)

1. **한국어로 서술한다.** 전문 용어는 **항상 한글 뒤에 영어를 괄호로 병기한다**
   — 예: `유전자 발현(Gene Expression)`, `염기서열(Base Sequence)`. 처음 한 번만이
   아니라, 용어를 새로 도입하는 자리(정의·표·그림 라벨·`\term{}`)마다 영어를 함께 적어
   독자가 영어 문헌과 곧바로 연결할 수 있게 한다. 이미 여러 번 반복되는 문장 속
   재언급까지 매번 붙일 필요는 없지만, 헷갈릴 여지가 있으면 병기한다.
2. **Semantic하게 쓴다.** 겉모양이 아니라 *의미*가 드러나게 쓴다.
   - 문단은 "무엇을 → 왜 → 어떻게"의 흐름으로 구성한다.
   - 개념·관계·예외를 명시적으로 구분해 서술한다(암시하지 않는다).
   - 강조는 의미 단위로만 한다(핵심 개념, 정의어).
3. **간단한 예제로 이론을 설명한다.** 추상 설명 뒤에는 항상 구체적인 작은 예를 붙여
   독자가 곧바로 이해할 수 있게 한다(`\begin{example}` 활용).
4. **비유를 일관되게 사용한다.** 한 번 정한 비유는 책 전체에서 같은 방식으로 쓴다.
   - 예(생명과학): 유전체=도서관 전체, 염색체=책 한 권, DNA=책을 이루는 긴 종이 띠,
     유전자=페이지/챕터, 염기서열=글자 배열, 모티프=의미 있는 단어, 발현=책을 읽고 실행.
5. **관계를 명시한다.** 계층·포함(담김) 관계는 말로만 두지 말고, 숫자와 함께 구체적으로
   적는다 — 예: "핵이 염색체 46개(23쌍)를 담는다", "염색체 1개 = DNA 분자 1개".
6. **섹션은 두괄식으로 연다.** 각 `\section`(주요 개념 단위)은 상세 설명에 들어가기 전에
   **결론부터** 요약하는 짧은 도입부를 둔다. 네 물음에 한 줄씩 답한다:
   **Why**(왜 쓰는가·무슨 문제를 푸는가) · **When**(언제 쓰는가·전제/조건) ·
   **Where**(어디에 쓰는가·적용 영역·안 맞는 경우) · **How**(어떻게 쓰는가·핵심 절차).
   - 형식: 섹션 제목 바로 아래 `\paragraph{In brief.}` + `description` 리스트로
     **한 줄에 하나씩**(row by row) 적는다 — 읽고 확인하기 쉽게:
     ```latex
     \paragraph{In brief.}
     \begin{description}
       \item[Why] ...
       \item[When] ...
       \item[Where] ...
       \item[How] ...
     \end{description}
     ```
   - 한 문단으로 뭉치지 않는다. 짧게(각 항목 한 문장). 상세는 그 뒤 본문에서 전개한다.
   - 개념 섹션에만 적용한다. 순수 개관/도입 섹션이나 억지로 네 물음이 안 맞는 곳은 생략.

## 2. 시각 자료 (적극 사용)

- 이해에 도움이 되면 **시각 자료를 적극적으로 넣는다** — 다이어그램, 표, 흐름도.
- 도구: **TikZ**(그림), `tabularx`/`booktabs`(표). 예제 코드는 `listings`.
- 자주 쓰는 시각화 패턴:
  - **계층/흐름**: 위→아래 화살표 다이어그램(중앙 도그마 등).
  - **포함(담김) 관계**: 중첩 상자(matryoshka) 다이어그램.
  - **대응 관계**: 3열 표(개념 · 관계 · 비유).
  - **과정**: 단계별 상자 + 화살표(스플라이싱 등).
- 모든 그림·표에는 `\caption`과 `\label`을 붙이고 본문에서 `\Cref`로 참조한다.

## 3. 구조 (LaTeX)

- 폴더 구조는 `book_ml`을 표준으로 삼는다:
  `main.tex` + `preamble.tex` + `parts/` + `chapters/`.
- 장(chapter)은 `chapters/NN-slug.tex`, 파트는 `parts/`에서 `\input`으로 묶는다.
- 교차 참조는 `\label`/`\Cref`로 연결한다(장·절·그림·표).
- 내용은 **점진적으로 추가**된다(사용자가 자료를 붙여주면 절/장으로 편입).
  기존 장에 자연스럽게 녹이고, 새 주제면 새 장을 만든다.

## 4. 빌드 (한글 = XeLaTeX)

로컬 TeX는 2026 **basic** 스킴이라 `kotex`, `tcolorbox`가 **없다**. 한글은 다음으로 처리한다.

- **XeLaTeX + fontspec + xeCJK**, 폰트는 macOS 시스템 폰트 **Apple SD Gothic Neo**.
- `preamble.tex`에 `\xeCJKsetup{CJKspace=true}` **필수** — 없으면 한글 단어 사이
  띄어쓰기가 사라진다.
- 빌드: `latexmk -xelatex main.tex` (pdflatex 아님).

### 자잘한 함정 (겪은 것)

- TikZ 상자에 짧은 한글 라벨을 넣을 때 `text width`(고정폭)를 주면 글자가 늘어난다
  (`세 포`). `minimum width`를 쓴다.
- TikZ 스타일 이름으로 `at`를 쓰지 않는다(예약어와 충돌).
- 콜아웃/박스는 `tcolorbox` 없이 순수 LaTeX(`\newenvironment` + `fcolorbox`)로 만든다.
- Latin Modern 은 fontconfig 에 이름이 없다(basic 스킴). `\setmainfont` 은 이름이 아니라
  파일명(`lmroman10-regular.otf`)으로 지정해야 XeLaTeX 이 찾는다.

## 5. Semantic 매크로 (preamble)

의미 단위로 표시하기 위한 매크로. 스타일은 preamble 한 곳에서만 바꾼다.

| 매크로 | 의미 | 용도 |
| --- | --- | --- |
| `\term{...}` | 정의·명명되는 개념 | 핵심 용어 |
| `\analogy{...}` | 비유 표현 | 도서관 비유 등 |
| `\contains` | 담김/포함 관계 기호(⊃) | 계층 관계식 |
